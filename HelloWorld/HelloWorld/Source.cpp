#include <iostream> // adds input/output streams library

int main() // will expect a return type of type int
{
	std::cout << "Hello World!" << std::endl; // std:: allows us to use objects with the std namespace / << outputs >> takes information from user / endl == \n 

	return 0;

}